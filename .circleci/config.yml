version: 2.1

executors:
  exe_nodejs:
    docker:
      - image: circleci/node:14.15-browsers
    working_directory: ~/repo

jobs:
  TheSetup:
    steps:
      - checkout
      - run: npm install
      - persist_to_workspace:
          root: .
          paths: .
  TheTest:
    steps:
      - attach_workspace:
          at: ~/repo
      - run: npm run test

workflows:
  version: 2
  Name-RunTests:
    jobs:
      - TheSetup
      - TheTest:
          requires:
            - TheSetup